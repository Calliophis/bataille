import { DifferentPlayersValidatorDirective } from './different-players-validator.directive';

describe('DifferentPlayersValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DifferentPlayersValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
