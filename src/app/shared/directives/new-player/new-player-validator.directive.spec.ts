import { NewPlayerValidatorDirective } from './new-player-validator.directive';

describe('NewPlayerValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NewPlayerValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
